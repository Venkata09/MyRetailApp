BASE_URL: https://redsky.target.com

server:
  port: 8899


spring:
  data:
    cassandra:
      keyspace-name: myretailapp
      contact-points: localhost
      port: 9042


kafka:
  bootstrap-servers: localhost:9092
  topic: test-topic
  message:
    publish-interval: 500